# Atomberg Fan Sleep Mod — Environment
#
# No server-side environment variables are required.
# All credentials (api_key, refresh_token, device_id) are passed
# per-request in the POST body from the mobile client.
#
# If you want to lock down the endpoint in the future, you could
# add a shared secret here:
# ENDPOINT_SECRET=your-shared-secret
